---
const TITLE = "Pagina no encontrada";
const DESCRIPTION = "La pagina que buscas no existe o ha sido movida.";
const BRAND = "#1e3a8a";

const HOME_URL = "/";
const EMAIL = import.meta.env.PUBLIC_CONTACT_EMAIL ?? "ventas@peruviansales.pe";
const PHONE = import.meta.env.PUBLIC_CONTACT_PHONE ?? "+51 999 999 999";
const PHONE_TEL = import.meta.env.PUBLIC_CONTACT_PHONE_TEL ?? "+51999999999";
---

<html lang="es">
  <head>
    <title>{TITLE}</title>
    <meta name="description" content={DESCRIPTION} />
    <meta name="robots" content="noindex" />
  </head>

  <body>
    <main class="relative min-h-screen overflow-hidden flex items-center justify-center bg-white">
      <!-- glows -->
      <div
        class="absolute -top-40 -right-40 h-96 w-96 rounded-full blur-3xl"
        style={`background: ${BRAND}0d;`}
        aria-hidden="true"
      />
      <div
        class="absolute -bottom-40 -left-40 h-96 w-96 rounded-full blur-3xl"
        style="background: rgba(34,197,94,0.10);"
        aria-hidden="true"
      />
      <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 h-[38rem] w-[38rem] rounded-full blur-3xl"
        style={`background: ${BRAND}08;`}
        aria-hidden="true"
      />

      <div class="relative z-10 mx-auto max-w-2xl px-4 sm:px-6 lg:px-8 text-center">
        <div class="mb-6">
          <span
            class="select-none leading-none block font-bold text-[10rem] sm:text-[14rem] lg:text-[18rem]"
            style={`color:${BRAND}; font-family: var(--font-montserrat, ui-sans-serif, system-ui);`}
          >
            404
          </span>
        </div>

        <h1
          class="mb-4 text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900"
          style="font-family: var(--font-montserrat, ui-sans-serif, system-ui);"
        >
          {TITLE}
        </h1>

        <p class="mb-8 text-lg text-slate-600 max-w-md mx-auto">
          Lo sentimos, la pagina que buscas no existe, ha sido movida o no tienes permisos para acceder a ella.
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <!-- home -->
          <a
            href={HOME_URL}
            class="group inline-flex items-center justify-center gap-2 rounded-full px-8 py-4 text-base font-semibold text-white transition-all duration-200 hover:shadow-xl active:scale-95"
            style={`background:${BRAND};`}
          >
            <!-- icono_home -->
            <svg
              class="h-5 w-5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M3 10.5l9-7 9 7V20a1 1 0 01-1 1h-5a1 1 0 01-1-1v-5a1 1 0 00-1-1h-2a1 1 0 00-1 1v5a1 1 0 01-1 1H4a1 1 0 01-1-1v-9.5z"
              />
            </svg>
            Ir al inicio
          </a>

          <!-- back -->
          <a
            href="javascript:history.back()"
            class="group inline-flex items-center justify-center gap-2 rounded-full border-2 px-8 py-4 text-base font-semibold text-slate-900 transition-all duration-200 hover:bg-slate-50 active:scale-95"
            style="border-color: rgba(15,23,42,0.15);"
          >
            <!-- left_arrow -->
            <svg
              class="h-5 w-5 transition-transform group-hover:-translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              aria-hidden="true"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M10 19l-7-7m0 0l7-7m-7 7h18"
              />
            </svg>
            Regresar
          </a>
        </div>

        <div class="p-2"></div>

        <div class="mt-12 pt-8 border-t" style="border-color: rgba(15,23,42,0.12);">
          <p class="text-sm text-slate-500 mb-4">¿Necesitas ayuda? Contáctanos:</p>

          <div class="flex flex-wrap items-center justify-center gap-6">
            <a
              href={`mailto:${EMAIL}`}
              class="inline-flex items-center gap-2 text-sm text-slate-600 hover:opacity-90 transition-opacity"
              style={`color:${BRAND};`}
            >
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="1.5"
                  d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75"
                />
              </svg>
              {EMAIL}
            </a>

            <a
              href={`tel:${PHONE_TEL}`}
              class="inline-flex items-center gap-2 text-sm text-slate-600 hover:opacity-90 transition-opacity"
              style={`color:${BRAND};`}
            >
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="1.5"
                  d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 002.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 01-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 00-1.091-.852H4.5A2.25 2.25 0 002.25 4.5v2.25z"
                />
              </svg>
              {PHONE}
            </a>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
